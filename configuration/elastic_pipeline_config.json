{
    "description": "Automatic timestamp ingestion",
    "processors": [
        {
            "set": {
                "field": "timestamp",
                "value": "{{_ingest.timestamp}}"
            }
        }
    ]
}